# To add dev dependencies, make changes here and then
# regenerate requirements-dev.txt by running
#
#   pip-compile requirements-dev.in
#
# and then commit this file and requirements-dev.txt.

flake8 ~= 7.0
pytest ~= 7.4
wheel ~= 0.41
coverage ~= 7.3
mypy ~= 1.7
pip-tools==7.3.0

# Deprecated is also specified in setup.cfg, but as a library dependency it is unpinned.
# For development and testing we still want to pin the dependency.
Deprecated
types-Deprecated

# Just needed for examples in documentation:
pandas