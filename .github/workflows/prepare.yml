name: Prepare Channel
on:
  workflow_call:
    inputs:
      channel:
        type: string
        required: true
      sync:
        type: boolean
        required: false
        default: false
      counter:
        type: number
        required: false
        default: 0
  workflow_dispatch:
    inputs:
      channel:
        description: Target Channel
        type: choice
        options:
          - nightly
          - beta
          - stable
        required: true
        default: nightly
      sync:
        description: Sync the Channel from Upstream?
        type: boolean
        required: false
        default: false
      counter:
        description: Version Counter
        type: number
        required: false
        default: 0
env:
  # Base dates on Harvard time
  TIME_ZONE: "America/New_York"

jobs:
  prepare:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Configure git user
        run: |
          git config --global user.name "$GITHUB_ACTOR"
          git config --global user.email "$GITHUB_ACTOR@users.noreply.github.com"

      - name: Set up Python
        uses: actions/setup-python@v3
        with:
          python-version: 3.9
          cache: pip

      - name: Install requirements
        run: pip install -r tools/requirements-release_tool.txt

      - name: Initialize channel
        env:
          SYNC_FLAG: ${{ inpugs.sync && '-s' || '-ns' }}
        run: python tools/release_tool.py init_channel -c ${{ inputs.channel }} $SYNC_FLAG -u 714-nightly-build

      - name: Configure channel
        run: python tools/release_tool.py config_channel -c ${{ inputs.channel }} -z "$TIME_ZONE" -i ${{ inputs.counter }}

      - name: Show changes
        run: git diff

      - name: Push changes
        run: |
          git commit -a -m "Sync and configure channel"
          git push --force origin ${{ inputs.channel }}
