% Print-build-only macros (LaTeX / xelatex via `leanblueprint pdf`).
% The `blueprint` LaTeX package (loaded in print.tex) supplies \lean, \leanok,
% \uses, \notready and the augmented theorem environments; nothing extra is
% needed here beyond build-specific helpers.
