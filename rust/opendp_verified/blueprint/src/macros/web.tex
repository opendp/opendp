% Web-build-only macros (plasTeX via `leanblueprint web`).
% The blueprint plasTeX plugin supplies \lean, \leanok, \uses, \notready and the
% augmented theorem environments. Build-specific helpers go here.
