% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transformations.R
\name{make_user_transformation}
\alias{make_user_transformation}
\title{user transformation constructor}
\usage{
make_user_transformation(
  input_domain,
  input_metric,
  output_domain,
  output_metric,
  function_,
  stability_map
)
}
\arguments{
\item{input_domain}{A domain describing the set of valid inputs for the function.}

\item{input_metric}{The metric from which distances between adjacent inputs are measured.}

\item{output_domain}{A domain describing the set of valid outputs of the function.}

\item{output_metric}{The metric from which distances between outputs of adjacent inputs are measured.}

\item{function_}{A function mapping data from \code{input_domain} to \code{output_domain}.}

\item{stability_map}{A function mapping distances from \code{input_metric} to \code{output_metric}.}
}
\value{
Transformation
}
\description{
Construct a Transformation from user-defined callbacks.
}
\details{
Required features: \code{contrib}, \code{honest-but-curious}

\strong{Why honest-but-curious?:}

This constructor only returns a valid transformation if for every pair of elements \eqn{x, x'} in \code{input_domain},
and for every pair \verb{(d_in, d_out)},
where \code{d_in} has the associated type for \code{input_metric} and \code{d_out} has the associated type for \code{output_metric},
if \eqn{x, x'} are \code{d_in}-close under \code{input_metric}, \code{stability_map(d_in)} does not raise an exception,
and \code{stability_map(d_in) <= d_out},
then \verb{function(x), function(x')} are d_out-close under \code{output_metric}.

In addition, for every element \eqn{x} in \code{input_domain}, \verb{function(x)} is a member of \code{output_domain} or raises a data-independent runtime exception.

In addition, \code{function} must not have side-effects, and \code{stability_map} must be a pure function.
}
\concept{transformations}
