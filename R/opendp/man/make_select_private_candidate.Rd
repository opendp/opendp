% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combinators.R
\name{make_select_private_candidate}
\alias{make_select_private_candidate}
\title{select private candidate constructor}
\usage{
make_select_private_candidate(measurement, stop_probability, threshold)
}
\arguments{
\item{measurement}{A measurement that releases a 2-tuple of (score, candidate)}

\item{stop_probability}{The probability of stopping early at any iteration.}

\item{threshold}{The threshold score. Return immediately if the score is above this threshold.}
}
\value{
A measurement that returns a release from \code{measurement} whose score is greater than \code{threshold}, or none.
}
\description{
Select a private candidate whose score is above a threshold.
}
\details{
Given \code{measurement} that satisfies ε-DP, returns new measurement M' that satisfies 2ε-DP.
M' releases the first invocation of \code{measurement} whose score is above \code{threshold}.

Each time a score is below \code{threshold}
the algorithm may terminate with probability \code{stop_probability} and return nothing.

\code{measurement} should make releases in the form of (score, candidate).
If you are writing a custom scorer measurement in Python,
specify the output type as \verb{TO=(float, "ExtrinsicObject")}.
This ensures that the float value is accessible to the algorithm.
The candidate, left as arbitrary Python data, is held behind the ExtrinsicObject.

Algorithm 1 in \href{https://arxiv.org/pdf/1811.07971.pdf#page=7}{Private selection from private candidates} (Liu and Talwar, STOC 2019).

Required features: \code{contrib}

\href{https://docs.rs/opendp/0.14.2-nightly.20260503.1/opendp/combinators/fn.make_select_private_candidate.html}{make_select_private_candidate in Rust documentation.}

\strong{Supporting Elements:}
\itemize{
\item Input Domain:   \code{DI}
\item Output Type:    \code{MI}
\item Input Metric:   \code{MaxDivergence}
\item Output Measure: \verb{Option<(f64, TO)>}
}

\strong{Proof Definition:}

\href{https://docs.opendp.org/en/nightly/proofs/rust/src/combinators/select_private_candidate/make_select_private_candidate.pdf}{(Proof Document)}
}
\concept{combinators}
