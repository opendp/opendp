% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/measurements.R
\name{make_user_measurement}
\alias{make_user_measurement}
\title{user measurement constructor}
\usage{
make_user_measurement(
  input_domain,
  input_metric,
  output_measure,
  function_,
  privacy_map,
  .TO = "ExtrinsicObject"
)
}
\arguments{
\item{input_domain}{A domain describing the set of valid inputs for the function.}

\item{input_metric}{The metric from which distances between adjacent inputs are measured.}

\item{output_measure}{The measure from which distances between adjacent output distributions are measured.}

\item{function_}{A function mapping data from \code{input_domain} to a release of type \code{TO}.}

\item{privacy_map}{A function mapping distances from \code{input_metric} to \code{output_measure}.}

\item{.TO}{The data type of outputs from the function.}
}
\value{
Measurement
}
\description{
Construct a Measurement from user-defined callbacks.
}
\details{
Required features: \code{contrib}, \code{honest-but-curious}

\strong{Why honest-but-curious?:}

This constructor only returns a valid measurement if for every pair of elements \eqn{x, x'} in \code{input_domain},
and for every pair \verb{(d_in, d_out)},
where \code{d_in} has the associated type for \code{input_metric} and \code{d_out} has the associated type for \code{output_measure},
if \eqn{x, x'} are \code{d_in}-close under \code{input_metric}, \code{privacy_map(d_in)} does not raise an exception,
and \code{privacy_map(d_in) <= d_out},
then \verb{function(x), function(x')} are d_out-close under \code{output_measure}.

In addition, \code{function} must not have side-effects, and \code{privacy_map} must be a pure function.

\strong{Supporting Elements:}
\itemize{
\item Input Domain:   \code{AnyDomain}
\item Output Type:    \code{AnyMetric}
\item Input Metric:   \code{AnyMeasure}
\item Output Measure: \code{AnyObject}
}
}
\concept{measurements}
