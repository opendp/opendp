% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod.R
\name{binary_search}
\alias{binary_search}
\title{Find the closest passing value to the decision boundary of \code{predicate}}
\usage{
binary_search(predicate, bounds = NULL, .T = NULL, return_sign = FALSE)
}
\arguments{
\item{predicate}{a monotonic unary function from a number to a boolean}

\item{bounds}{a two-element list of optional lower and upper bounds on the input
of \code{predicate}; use \code{NULL} for either bound to infer it}

\item{.T}{type of argument to \code{predicate}, one of float or int; when set,
takes precedence over the type inferred from \code{bounds}}

\item{return_sign}{if True, also return the direction away from the decision boundary}
}
\value{
the discovered parameter within the bounds
}
\description{
Missing bounds are inferred. Use \code{NULL} for either element of \code{bounds} to
conduct a one-sided search, or omit \code{bounds} to conduct an exponential search.
}
\examples{
binary_search(\(x) x <= -5L, bounds = list(-10L, NULL))
}
\concept{mod}
