# Check membership in a `domain`.

Check membership in a `domain`.

## Usage

``` r
member(this, val)
```

## Arguments

- this:

  The domain to check membership in.

- val:

  A potential element of the domain.

## Value

bool
