# Construct an instance of `VectorDomain`.

Construct an instance of `VectorDomain`.

## Usage

``` r
vector_domain(atom_domain, size = NULL)
```

## Arguments

- atom_domain:

  The inner domain.

- size:

  undocumented

## Value

VectorDomain
