# Retrieve the size of vectors in the vector domain.

Retrieve the size of vectors in the vector domain.

## Usage

``` r
vector_domain_get_size(vector_domain)
```

## Arguments

- vector_domain:

  The vector domain from which to retrieve the size
