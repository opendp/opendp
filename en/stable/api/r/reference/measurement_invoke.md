# Invoke the `measurement` with `arg`. Returns a differentially private release.

Invoke the `measurement` with `arg`. Returns a differentially private
release.

## Usage

``` r
measurement_invoke(this, arg)
```

## Arguments

- this:

  Measurement to invoke.

- arg:

  Input data to supply to the measurement. A member of the measurement's
  input domain.
