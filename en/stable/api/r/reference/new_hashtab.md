# create an instance of a hashtab from keys and values

create an instance of a hashtab from keys and values

## Usage

``` r
new_hashtab(keys, vals)
```

## Arguments

- keys:

  a vector of keys

- vals:

  a vector of values
